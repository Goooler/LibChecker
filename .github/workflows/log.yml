name: Android CI

on:
  push:
  pull_request:

jobs:
  log-message:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Check style
        uses: gradle/gradle-build-action@v2
      - run: |
          echo The commiter is COMMITER
        env:
          COMMITER: ${{ github.event.issue.head_commit.committer.name }}
