name: Android CI

on:
  push:
  pull_request:

jobs:
  log-message:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          echo The message is COMMITER
        env:
          COMMITER: ${{ github.event.head_commit.commiter }}
